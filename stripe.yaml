stripe:
  image: 'stripe/stripe-cli:latest'
  network_mode: host
  environment:
    STRIPE_API_KEY: ${STRIPE_SECRET_API_KEY}
    STRIPE_DEVICE_NAME: ${STRIPE_DEVICE_NAME}
  command: listen --forward-to localhost:5000/webhook
